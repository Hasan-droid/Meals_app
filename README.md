# meals

Simple Flutter app that displays a list of meals and allows the user to filter them by categories and mark them as favorites.

## Getting Started

This project is a starting point for a Flutter application.

## Run the app

#### Clone the repository

```bash
git clone
```

#### Install dependencies

```bash
flutter pub get
```

#### Run the app

```bash
flutter run
```
